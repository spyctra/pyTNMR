# pyTNMR
python code to control Tecmag's TNMR software.

pyTNMR allows users to avoid using 2D tables by automating variable updates. This allows the user to process data (with spyctra_v6) as TNT files are collected.
